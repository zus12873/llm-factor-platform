# 使用说明

**更新日期**：2026-08-28  
**适用版本**：GitHub `main` `8d771b9`（含研报工作流、因子库、字段复权语义、离线 GitHub Actions）。Compose 真实冒烟仍未执行。

本文讲两件事：平台能做什么，以及怎么用。

> ⚠️ **先读这一段。** 平台已经在公司内网真实连接 Wind，并跑通真实取数 → 签名
> manifest → 隔离 Worker → 三层校验。仓库里现存的真实模型验收是 **Kimi Coding
> Plan / k3**（自然语言、研报、浏览器五场景），见
> [2026-08-14 Coding Plan 终验](acceptance/2026-08-14-coding-final/README.md)。
> 按量 / Open Platform 专项目录不在本仓库，不要凭断裂链接宣称按量已通过或已 401。

---

## 一、这个平台解决什么问题

研究员有一个想法：「ROE 高的股票未来收益更好」。从这句话到一个能跑、能复现、
能被别人检查的因子，中间有一串容易出错的步骤：

- 「ROE」是 ROE_TTM、ROE_摊薄、还是扣非 ROE？
- 它在 Wind 的哪张表哪个字段？单位是百分数还是比值？
- 财报数据什么时候才公开？用报告期还是公告日？
- T 日收盘算出的因子，能在 T 日交易吗？
- 三个月后重跑，数字不一样了，是代码错了还是数据被修订了？

这些问题的共同特点是：**答错了不会报错**。因子照样算得出来，图照样能画，
只是数字是错的，而且没有任何环节会发现。

平台把这些判断点全部显式化：**能确定的自动做，不能确定的停下来问，
永远不猜。**

---

## 二、功能清单

### 2.1 从想法到因子（P0 主流程）

| 功能 | 说明 |
|---|---|
| 自然语言解析 | 一句研究想法 → 结构化的因子定义（`FactorSpec`） |
| 歧义识别与阻塞 | 「盈利质量」这类多口径表述会**停下流程**并给出候选，不替你选 |
| 规范公式渲染 | 模型只产出 AST，后端确定性渲染出你确认的那条公式 |
| 字段发现 | 本地授权字典含 7,478 条释义元数据；公开仓库目录含 7,487 行标识符、7,404 个唯一表字段组合；支持中文检索、含义、单位和时间角色 |
| 字段验证 | Schema 验证（字段是否存在）与数据存在性验证（是否有数据）**分开** |
| 取数规划 | 自动补齐历史指数成分、预热区间、ST/停牌过滤 |
| 签名 manifest | 平台实际执行的唯一对象，可验签、可复现 |
| 隔离执行 | 无密钥 Worker，`network_mode: none` |
| 三层校验 | 数据层、公式层、结果层，分别报告阻塞项与提示项 |
| 代码导出 | 可读的 `factor.py`，供你在自己的环境复现 |

### 2.2 研报入口

| 功能 | 说明 |
|---|---|
| PDF 解析 | 文本型中英文研报，保留页码与坐标作为证据 |
| 受限 OCR | 仅对无文本层的页面运行，200 DPI，每份研报上限 50 页 |
| 因子抽取 | 从研报中提取因子定义，每条结论必须引用页内证据 |
| 人工确认闸门 | 置信度不足或版式不可靠时**强制人工确认**，不进入执行 |
| 进入因子工作流 | 抽取 JSON 落盘；确认公式并填可信信封后进入共用工作台。**不跳过公式确认** |

### 2.3 因子检验与资产管理

| 功能 | 说明 |
|---|---|
| IC / Rank IC | 含离散度，不只报均值 |
| 分位收益与多空价差 | 单调性作为事实报告，不作为判断 |
| 换手率 | 按成分变动计算，不是权重漂移 |
| 因子库 | 不可变版本；工件复制入库而非引用；页面列出入库时的 `review_status`；`disputed` 拒绝发布 |
| 复现记录 | 记录取数时刻、数据快照与 9 个组件版本 |
| 多因子合成 | 方向统一 → 标准化 → 加权，顺序固定 |
| 指数权重 | Top N、等权或因子加权、周度/月度调仓、CSV 导出 |

### 2.4 治理与安全

| 功能 | 说明 |
|---|---|
| 口径注册表 | 单一 YAML，三值复核状态，非程序员可读可签字 |
| 三值闸门 | `unreviewed` 可试算、可入库但标注「未复核」；`reviewed` 可发布；`disputed` **禁止执行也禁止入库** |
| 数据出境边界 B4 | Wind 原始数据、密钥、研报全文默认不外发 |
| 全本地模式 | `LOCAL_ONLY_MODE=true` 时禁止一切外部模型调用 |
| 两角色权限 | `admin` 管用户与口径复核，`researcher` 建因子 |

---

## 三、安装

需要 [uv](https://docs.astral.sh/uv/)（Python 3.11）与 Node 22+。

```bash
git clone https://github.com/zus12873/llm-factor-platform.git
cd llm-factor-platform

uv sync --project backend
npm --prefix frontend install
```

验证安装：

```bash
uv run --project backend pytest backend/tests -q
npm --prefix frontend test -- --run
```

这两条**不需要任何凭据**。

最新离线门禁见 [`acceptance/2026-08-27-offline-gates.md`](acceptance/2026-08-27-offline-gates.md)：本机 pytest **701 passed** / vitest **41 passed**；GitHub Actions 在 `8d771b9` 为 **success**（pytest **697 passed, 4 skipped**，无许可字典）。完整真实验收（真实 Wind / Kimi Coding Plan / 浏览器）仍以 [`acceptance/2026-08-14-coding-final/README.md`](acceptance/2026-08-14-coding-final/README.md) 为准（当时后端 647 / 12 skipped、前端 29）；本轮没有重跑真实 Wind / Kimi / Compose。

---

## 四、命令行操作流程

### 4.1 看看平台会做什么（最快的上手方式）

```bash
# 列出 37 个内置案例
uv run --project backend factor-platform list-cases

# 跑一个清晰的案例，看它走完整条链
uv run --project backend factor-platform run-case momentum_20d
```

输出：

```
parse    : rank(rolling_return(close, window=20))
clarify  : no blocking question
plan     : 4 步 — wind.index_components, wind.is_st_stock,
                  wind.is_suspended, wind.get_price
           预热区间自 2022-11-25 起
fetch    : 跳过（--real-wind 未给出；这是唯一需要凭据的阶段）
```

**注意「预热区间自 2022-11-25 起」**：请求的区间是 2024-01-01 开始，
但 20 日动量在第 1 天没有值，规划器自动往前多取了历史。你没要求，
但少了它前 20 天全是 NaN。

### 4.2 看看它拒绝做什么

```bash
uv run --project backend factor-platform run-case profitability_ambiguous
```

```
parse    : rank(profitability)
clarify  : blocked on profitability_definition
           （想法不够明确，按设计在此停下）
```

「盈利质量」有至少三种口径。平台不选，它问。

### 4.3 跑验收套件

```bash
uv run --project backend factor-platform run-case-suite --set golden \
  --report /tmp/golden.json
```

报告分组给出指标，并**明确列出无法测量的部分及原因** —— 不会用 0 或 1
假装测过。

### 4.4 构建 Wind 字段目录（需要 Wind 数据字典）

Wind 数据字典受商业授权保护，不随仓库分发。若你有授权：

```bash
# 1. 把字段索引放到 windquery/windquery/references/wind_field_index.md
uv run --project backend factor-platform build-wind-catalog \
  --source windquery/windquery/references/wind_field_index.md \
  --output backend/data/generated/wind_fields.jsonl

# 2. 解析数据字典，合并出字段元数据（中文名、单位、频率、市场）
uv run --project backend factor-platform sync-wds-metadata \
  --source "windquery/windquery/references/wind字典" \
  --index backend/data/generated/wind_fields.jsonl \
  --units backend/data/wind_field_units.yaml \
  --output backend/data/generated/wind_metadata.jsonl
```

第二条会如实报告覆盖率：

```
wrote 7478 fields to backend/data/generated/wind_metadata.jsonl
  described by dictionary : 7000 (93.6%)
  no metadata recovered   : 478 (kept and flagged, not dropped)
```

478 个字段没有元数据 —— 它们**仍然可检索**，只是排序时没有中文描述可用。
标注为「无元数据」而不是被丢掉。

### 4.5 验证一个字段（**需要 Wind 凭据**）

```bash
uv run --project backend factor-platform verify-field \
  ashareeodprices s_dq_close --shape point_range
```

两段独立报告：

```
schema: schema_valid_data_present — column exists with a compatible time role
sample: schema_valid_data_present — 60/60 sampled rows are non-null
  plan  : 3 securities × 20 trading_day
```

**「字段不存在」和「区间内无数据」是两件事。** 后者不阻塞 —— 字段可能完全有效，
只是你选的区间没有数据，那是你的判断不是平台的。

---

## 五、Web 界面操作流程

### 5.1 启动

```bash
# 终端 1：后端
APP_ENV=test uv run --project backend uvicorn --factory \
  factor_platform.main:create_app --host 127.0.0.1 --port 8000

# 终端 2：前端
npm --prefix frontend run dev
```

打开 http://localhost:5173

### 5.2 读懂顶部横幅

右上角始终显示组件状态：

| 显示 | 含义 |
|---|---|
| 🟢 全部就绪 | Wind 与模型均已配置 |
| 🟠 离线模式：wind、llm | **这两项未配置，结果基于替身数据** |
| 🔴 后端不可达 | 后端没起来或崩了 |

这不是装饰。看不到「离线模式」提示的话，你会把替身数据的运行读成真实运行。

### 5.3 完整操作流程

**第一步：填写研究想法**

上方四个结构化字段（资产类型、股票池、频率、区间）是**可信信封**——
模型不会改动它们。只有下方的自由文本会发给模型。

**第二步：处理澄清**

如果想法有多种口径，界面会弹出澄清卡片，列出候选并要求你选。
**没有「跳过」也没有「使用默认」**——这里的默认值就是整个设计要避免的那个猜测。

候选项来自口径注册表，已标记为 `disputed` 的口径不会出现在选项里。

**第三步：确认公式**

卡片主体是**正式公式**（由 AST 确定性渲染），下方是模型的自然语言说明，
带「仅供阅读，不参与执行」标签。

> 这两者的区分是刻意的：你确认的必须是会执行的那条，而不是它的解释。

**第四步：确认字段绑定**

表格逐列给出：变量、表、字段、**复权**、含义、**口径说明**、**单位**、时间角色、来源层。

动量 / 收益率 / 波动类检索会把后复权收盘价排前并贴标签，但 **`收盘价` 别名仍是 `s_dq_close`**，`close` 不等于 `adj_close`。查询「收盘价」时两边都会列出，必须人工确认，平台不替你改写。生产 `discover_fields` → `FieldSearch.search` 会注入优先字段 `s_dq_close`，并在默认 `limit=5` 下为其预留席位，避免拥挤结果把未复权收盘价挤出列表。规划器的 `adjust_type` 跟**已确认字段**：确认 `s_dq_close`（以及 `s_dq_preclose`）则取 `none`，即使数据规则里 `use_adjusted_price` 为 True；确认 `s_dq_adjclose` 则取 `post`；确认 `s_dq_adjclose_backward` 则取 `pre` 且走 `get_price`。

**特别注意「单位」列。** 标注「单位未知」的字段，其量级错误在下游**不会被任何
检查发现** —— 市值按元而非万元读取是 10,000 倍误差，而结果看起来完全正常。

**第五步：查看计划与执行**

计划会列出全部步骤及预热区间。执行进入无密钥 Worker。

**第六步：读结果**

结果面板同时给出覆盖率与校验发现。校验发现按级别分组：

- **红色（阻塞）** —— 必须处理才能发布
- **黄色（提示）** —— 需要知道，但不阻止

如果因子引用了未复核口径，面板顶部会标「含未复核口径，入库将标注「未复核」」。
未复核**不**禁止发布；有争议（`disputed`）口径禁止发布。会话完成后可点「发布到因子库」。

### 5.4 修订与级联失效

改公式、改字段、改区间都会触发**级联失效**：

| 你改了什么 | 什么会被清空 |
|---|---|
| 研究想法 | 全部下游，包括因子定义本身 |
| 公式 | 字段选择、计划、构建产物、结果 |
| 字段 | 计划、构建产物、结果 |
| 区间 / 股票池 | 计划、结果（构建产物保留） |
| 预处理 | 构建产物、结果（计划保留） |
| 时间口径 | 计划、构建产物、结果 |

分界线是：**改「取哪些行」失效计划，改「怎么算」失效构建产物。**

这不是为了麻烦你。没有级联失效的话，你改完公式后界面上仍挂着旧的字段选择和
旧的结果 —— 那个因子的各个部分**从未被一起计算过**，而数据里没有任何东西
表明这一点。

执行中的任务不接受修订，必须先取消 —— 否则会和 Worker 抢工件写入。

---

## 六、研报入口

### 6.1 上传

界面「研报提取」页支持拖拽 PDF。限制：50 MiB、200 页、非加密。

### 6.2 能力边界（界面上也会显示）

> 首期支持正文可复制、变量定义明确的中英文文本型研报。
> 图片公式、复杂数学排版与扫描版仅提取正文线索，公式需人工确认。

这不是免责声明，是设计决定。通用 OCR 会把数学记号弄坏成**仍能解析**的样子 ——
漏一个负号，因子方向就反了。所以 `formula_is_trustworthy("ocr")` 恒为 `False`。

### 6.3 抽取结果

三种情况：

| 状态 | 含义 | 你要做什么 |
|---|---|---|
| 已抽取（高置信度） | 公式识别可信 | 检查证据页码后继续 |
| 需人工确认 | 置信度不足、多栏版式或含图片 | **手动输入或修正公式** |
| 抽取失败 | 模型出错或无相关段落 | 依据仍给出的证据手动输入 |

**「进入因子工作流」在公式未经确认、可信信封的开始/结束日期未填齐前保持禁用。**
低置信度的抽取结果在屏幕上看起来和好结果一模一样，按钮状态是唯一在执行这条规则的东西。

失败时仍然返回证据 —— 失败且不给证据，等于让你从零手工重来。

### 6.4 进入因子工作流

上传时服务端把抽取结果写成 `{artifact_id}.extraction.json`，与 PDF 同目录。
点「进入因子工作流」时：

1. 填写与研究想法页相同的**可信信封**（资产类型、股票池、频率、区间）。信封不会发给模型改写。
2. 前端调用 `POST /api/reports/{artifact_id}/sessions`，**不**把抽取 JSON 回传给通用 `POST /api/sessions`。
3. 高置信度抽取：后端用已存 AST 生成 FactorSpec，**不再**把研报全文发给模型（B4）。
4. 需人工确认：必须提交手输公式；解析器只看到这条公式文本，不是研报正文。
5. 成功后跳到 `/workbench/{session_id}`。会话落在 `needs_clarification` 或 `waiting_formula_confirmation`，**不会**直接进入 `searching_fields`。工作台仍要确认公式和字段。

---

## 七、因子库

只接受已经跑完的工作台会话。结果面板在会话状态为 `completed` 且结果状态为 completed 时给出「发布到因子库」。

| 口径 | 发布 |
|---|---|
| `reviewed` | 可以 |
| `unreviewed` | 可以，入库标签为「未复核」 |
| `disputed` | **禁止**（按钮禁用；后端 422 `publish_refused`） |

工件复制到 `data/artifacts/library/{factor_id}/v{n}/`，不是引用当次运行目录。
「因子库」页列出每个因子的最新版本（名称、版本、**入库时存下的**复核状态、时间、会话）。
点开看正式公式、manifest / program / result 哈希、来源输入、工件路径。不在浏览器里拉 Parquet。页面**不**用当前口径注册表重算复核状态。

对应 HTTP：`GET /api/library`、`GET /api/library/{factor_id}/v/{version}`、`POST /api/library`（body：`session_id`，可选 `factor_id`）。

---

## 八、Worker 与队列运维

### 8.1 三条命令

```bash
export MANIFEST_SIGNING_KEY="<与后端相同的密钥>"

# 领一个任务跑完就退出
uv run --project backend factor-worker run-once \
  --job-root data/runtime/jobs \
  --artifact-root data/artifacts \
  --input-root data/artifacts/inputs

# 排空队列
uv run --project backend factor-worker serve --job-root ... --artifact-root ... --input-root ...

# 处理租约过期的任务
uv run --project backend factor-worker recover \
  --job-root data/runtime/jobs --artifact-root data/artifacts
```

### 8.2 Worker 持有什么

**只有 manifest 签名密钥，而且只用来验签。** 它没有数据库凭据、没有模型密钥。
被攻破的 Worker 什么也够不着。

### 8.3 recover 的三种处置

租约过期**不等于**该重跑：

| 情况 | 处置 |
|---|---|
| 结果完整（Parquet **与** result.json 都在） | 标记完成 |
| 无结果，未达重试上限 | 重新入队 |
| 无结果，已达上限（2 次） | 标记失败 |

Worker 可能在写完完整结果**之后**才死。盲目重跑会重复计算并覆盖你可能
已经打开过的工件。

---

## 九、接入真实数据

### 9.1 前置：凭据轮换

⚠️ **在做任何事之前**，必须由具备数据库管理权限的人轮换已暴露的 Wind 凭据。

⚠️ **不要把凭据值粘贴进对话、聊天、日志或任何文档。** 本仓库是公开仓库。

### 9.2 配置

```bash
cp .env.example .env
# 编辑 .env，填入轮换后的值
```

| 变量 | 说明 |
|---|---|
| `WIND_ENABLED=true` | 打开真实取数 |
| `WIND_HOST` / `WIND_PORT` / `WIND_USER` / `WIND_PASSWORD` / `WIND_DATABASE` | Wind MySQL 只读副本 |
| `SESSION_COOKIE_SECRET` | 32+ 字节随机串，`python -c "import secrets; print(secrets.token_urlsafe(32))"` |
| `MANIFEST_SIGNING_KEY` | 同上，另生成一个 |
| `KIMI_CODING_BASE_URL` / `KIMI_CODING_API_KEY` | Coding Plan 端点（优先） |
| `KIMI_METERED_API_KEY` / `KIMI_MODEL` | 按量 API 兜底 |
| `LOCAL_ONLY_MODE=true` | 禁止一切外部模型调用 |

### 9.3 接入后的第一批验证

最新真实取数与模型闭环见 [`acceptance/2026-08-14-coding-final/README.md`](acceptance/2026-08-14-coding-final/README.md)。
优先三条：

```bash
# 1. 三种查询形态各验一个字段
factor-platform verify-field ashareeodprices s_dq_close --shape point_range
factor-platform verify-field asharefinancialindicator s_fa_roe --shape report_period
factor-platform verify-field aindexmembers s_con_indate --shape interval_overlap

# 2. 端到端真实取数
factor-platform run-case momentum_20d --real-wind

# 3. 单位核对（见下）
```

### 9.4 已完成人工确认的口径

带教老师已确认当前定义可用（[`acceptance/metric-review-evidence.md`](acceptance/metric-review-evidence.md)），后续也已明确授权同步运行时状态。`backend/data/metric_definitions.yaml` 中确认清单对应的 11 个登记现为 `reviewed`，界面和发布闸门均按该状态工作。

以下三条仍是容易造成静默量级错误的重点使用注意事项，但不再属于“待确认”状态：

| 口径 | 量级风险 | 错了会怎样 |
|---|---|---|
| `ROE_TTM` | 已确认按百分数使用（15.0 表示 15%），不得当作 0.15 比值 | 误当比值会差 100 倍 |
| `REVENUE_YOY` | 已确认按 `s_fa_yoy_or` 当前 Wind 字段口径使用，不得与其他单季度/累计字段混用 | 混用会变成不同因子 |
| `s_dq_volume` | 已确认按“手”使用；换算股份时按 1 手 = 100 股 | 漏换算或重复换算会差 100 倍 |

另有两条已登记为 `disputed`、平台直接拒绝，且不在本次确认清单内：
`FLOAT_MV`（`float_a_shr` 是股本不是市值）、
`CFO_WRONG_MAPPING`（现金流量表的 `net_profit` 是间接法起始行）。

---

## 十、部署

```bash
cp deploy/compose.env.example deploy/compose.env
# 填入 SESSION_COOKIE_SECRET、MANIFEST_SIGNING_KEY；WIND_ENABLED 可保持 false
docker compose --env-file deploy/compose.env -f deploy/compose.yaml up --build -d
```

访问 http://127.0.0.1:8080

`deploy/compose.env` 已列入 `.gitignore`，**不要提交**填好的密钥。

⚠️ **前端绑定 127.0.0.1** —— 本原型没有 TLS，也未做暴露加固。
发布到 `0.0.0.0` 等于把一个默认无认证的研究工具挂上网络。

⚠️ **Docker 构建与 Compose 真实冒烟尚未执行**（本机 `docker` 命令不存在，
`docker info` 为 `command not found`，exit 127）。Compose 契约 13 条测试已离线通过。
契约通过 **不等于** 冒烟通过。证据见 [`acceptance/compose-smoke.md`](acceptance/compose-smoke.md)。
有 Docker 后再按该文件的命令跑一遍，用真实输出覆盖「未执行」。

---

## 十一、常见问题

**点了「开始解析」没反应？**  
看顶部横幅。若显示「离线模式：llm」，说明模型未配置 —— 界面会给出
`llm_provider_unavailable` 错误码与处理建议。应用不会隐式调用 Fake Provider。

**为什么因子前 20 行全是 NaN？**  
20 日动量在窗口未满时**应该**没有值。用不足的历史凑出一个数，会让回测
前段悄悄变成另一个因子。这是正确行为。

**改了公式，字段选择怎么没了？**  
级联失效，见 [5.4](#54-修订与级联失效)。

**三个月后重跑，数字不一样了？**  
先看因子库里的复现记录。Wind 会**追溯修订财报** —— 同一份程序在不同时间
跑出不同结果，两次都是对的。`explains_difference_from()` 会告诉你是代码变了、
数据变了、还是口径变了。

**隐藏验收集在哪？**  
`backend/data/hidden_cases/` 有 10 个历史案例（2026-08-27 从本机工作副本入库）。
入库后任何人都能读到，**不再是盲测**。不得把对着它们跑通的结果写成 2026-08-10
原隐藏验收。`docs/acceptance/2026-08-10/hidden.json` 是当时的运行报告，不是输入。

**能直接下单吗？**  
不能。指数产物只是调仓日权重，未计交易成本、冲击成本、涨跌停与停牌限制。
产物里自带这句声明。

---

## 附：所有命令速查

| 命令 | 需要凭据 |
|---|---|
| `factor-platform list-cases` | 否 |
| `factor-platform parse-case <id>` | 否 |
| `factor-platform run-case <id>` | 否 |
| `factor-platform run-case <id> --real-wind` | **是** |
| `factor-platform run-case-suite --set golden\|hidden` | 否 |
| `factor-platform build-wind-catalog` | 否（需 Wind 字典文件） |
| `factor-platform sync-wds-metadata` | 否（需 Wind 字典文件） |
| `factor-platform verify-field` | **是** |
| `factor-worker run-once\|serve\|recover` | 仅签名密钥 |
